# vlib Test config 
# OS Environement Variables are Allowed

database:
   engine: mysql
   host: localhost
   db: vlibtests
   user: vlibtests
   passwd: bogangles
   dictcursor: true
   timezone: America/New_York

logging:
   filename: X
   level: DEBUG

email:
   server  : X
   username: X
   password: X
   fromaddr: X

shell: $SHELL
secret: toyboat\$
